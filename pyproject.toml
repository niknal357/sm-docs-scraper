[project]
name = "sm-docs-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "markdown>=3.10.2",
    "pagefind[bin]>=1.5.2",
    "pygments>=2.20.0",
]
